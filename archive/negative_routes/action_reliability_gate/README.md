# Closed: action-conditioned reliability gate

Real nuScenes evaluation found that the full action-conditioned model underperformed the detector-only baseline, timing controls were stronger, calibration worsened, and the prespecified gate criteria failed. This route is retained only as negative evidence. The safety tag contains the complete original output tree.
