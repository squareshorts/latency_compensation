# Closed: short-horizon nuScenes annotation route

The timing audit used every real CAM_FRONT frame and established that intermediate sweeps have no genuine boxes or persistent identities. The original 100–300 ms object endpoint could not be evaluated without forbidden annotation interpolation. A genuine 500 ms keyframe endpoint was subsequently evaluated separately.
