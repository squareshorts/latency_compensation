# Closed: visual-residual dual loop

At 500 ms the visual-residual method strongly improved stale boxes but lost to the simpler causal inertial plus historical object-motion model and exceeded the 20 ms overhead target. The B4 result was promoted to the active nuScenes real-data feasibility result; the residual-flow hypothesis is closed as a primary route.
