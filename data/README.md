# Data

Raw datasets are never committed. The verified nuScenes v1.0-mini data remain at `C:\work\auto\data\nuscenes`. Future AV2 payloads belong under `C:\work\auto\data\av2` and are ignored by Git; only fixed log lists, download manifests, and presence audits are tracked.
